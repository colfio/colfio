{
  "name": "ecs-lite",
	"version": "0.2.3",
	"license": "MIT",
  "scripts": {
    "start": "parcel examples/*.html --out-dir www --open",
    "start_no_map": "parcel examples/*.html --out-dir www --open --no-source-maps",
    "deploy": "parcel build examples/*.html --no-source-maps",
    "tests": "parcel examples/tests.html --out-dir www --open"
  },
  "devDependencies": {
    "parcel-bundler": "^1.8.1",
    "typescript": "^2.8.3",
    "pixi.js": "^5.0.0"
  },
  "dependencies": {
  }
}
